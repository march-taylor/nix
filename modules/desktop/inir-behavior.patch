--- a/shell.qml
+++ b/shell.qml
@@ -10,13 +10,11 @@
                 // Waffle always opens its own Win11-style settings window
                 Quickshell.execDetached([Quickshell.shellPath("scripts/inir"),
                     "waffle-settings-window"])
-            } else if (Config.options?.settingsUi?.overlayMode ?? false) {
-                // ii overlay mode — toggle inline panel
+            } else {
+                // Material ii always uses the inline overlay. Keeping settings
+                // in the main shell process also prevents stale secondary
+                // Config mirrors from overwriting user settings.
                 GlobalStates.settingsOverlayOpen = !GlobalStates.settingsOverlayOpen
-            } else {
-                // ii window mode (default) — launch separate process
-                Quickshell.execDetached([Quickshell.shellPath("scripts/inir"),
-                    "settings-window"])
             }
         }
         function toggle(): void {
@@ -24,8 +22,8 @@
         }
     }
 
-    // Settings overlay panel (loaded only when overlay mode is enabled)
+    // Material ii settings live in the main shell process.
     LazyLoader {
-        active: Config.ready && (Config.options?.settingsUi?.overlayMode ?? false)
+        active: Config.ready && (Config.options?.panelFamily ?? "ii") !== "waffle"
         component: SettingsOverlay {}
     }
--- a/modules/bar/BarContent.qml
+++ b/modules/bar/BarContent.qml
@@ -64,7 +64,7 @@
                 monochromeIcon: true,
                 text: Translation.tr("Settings"),
                 action: () => {
-                    Quickshell.execDetached([Quickshell.shellPath("scripts/inir"), "settings"])
+                    GlobalStates.settingsOverlayOpen = !GlobalStates.settingsOverlayOpen
                 },
             },
         ]
--- a/modules/sidebarLeft/widgets/GlanceHeader.qml
+++ b/modules/sidebarLeft/widgets/GlanceHeader.qml
@@ -192,10 +192,14 @@
                     onClicked: {
                         const isWaffle = (Config.options?.panelFamily === "waffle" && Config.options?.waffles?.settings?.useMaterialStyle !== true);
-                        const settingsPath = isWaffle ? Quickshell.shellPath("waffleSettings.qml") : Quickshell.shellPath("settings.qml");
-                        const pageIndex = isWaffle ? 6 : 5; // Modules (Waffle) vs Interface (ii)
-                        const section = isWaffle ? Translation.tr("Widgets Panel") : Translation.tr("Widgets");
-
-                        Quickshell.execDetached(["/usr/bin/env", "QS_SETTINGS_PAGE=" + pageIndex, "QS_SETTINGS_SECTION=" + section, Quickshell.shellPath("scripts/inir"), isWaffle ? "waffle-settings-window" : "settings-window"]);
+                        if (isWaffle) {
+                            const pageIndex = 6;
+                            const section = Translation.tr("Widgets Panel");
+                            Quickshell.execDetached(["/usr/bin/env", "QS_SETTINGS_PAGE=" + pageIndex, "QS_SETTINGS_SECTION=" + section, Quickshell.shellPath("scripts/inir"), "waffle-settings-window"]);
+                        } else {
+                            // Material ii settings are part of the live shell.
+                            // Never start a second Config mirror from this button.
+                            GlobalStates.settingsOverlayOpen = true;
+                        }
                     }
 
                     contentItem: Item {
--- a/modules/common/functions/ShellExec.qml
+++ b/modules/common/functions/ShellExec.qml
@@ -52,9 +52,9 @@
             shift 2
             if [ -x "$systemd_run" ]; then
                 if [ -n "$desc" ]; then
-                    "$systemd_run" --user --scope --quiet --collect --property="Description=$desc" -- "$@" && exit 0
+                    "$systemd_run" --user --quiet --collect --same-dir --property="Description=$desc" -- "$@" && exit 0
                 else
-                    "$systemd_run" --user --scope --quiet --collect -- "$@" && exit 0
+                    "$systemd_run" --user --quiet --collect --same-dir -- "$@" && exit 0
                 fi
             fi
             exec "$@"
--- a/modules/common/Config.qml
+++ b/modules/common/Config.qml
@@ -667,4 +667,5 @@
                 property JsonObject palette: JsonObject {
                     property string type: "auto" // Allowed: auto, scheme-content, scheme-expressive, scheme-fidelity, scheme-fruit-salad, scheme-monochrome, scheme-neutral, scheme-rainbow, scheme-tonal-spot
                     property string accentColor: "" // Seed color hex for scheme variant (e.g. "#ab1234"), empty = use theme primary
+                    property string mode: "dark" // User-selected shell mode; never infer it from wallpaper brightness.
                 }
--- a/defaults/config.json
+++ b/defaults/config.json
@@ -109,4 +109,5 @@
         "palette": {
             "type": "auto",
-            "accentColor": ""
+            "accentColor": "",
+            "mode": "dark"
         },
--- a/services/MaterialThemeLoader.qml
+++ b/services/MaterialThemeLoader.qml
@@ -50,3 +50,4 @@
     function setDarkMode(dark: bool): void {
+        Config.setNestedValue("appearance.palette.mode", dark ? "dark" : "light")
         darkModeProc.command = [
             "/usr/bin/bash",
--- a/services/ThemeService.qml
+++ b/services/ThemeService.qml
@@ -178,5 +178,8 @@
             const themingPath = Wallpapers.currentThemingWallpaperPath()
             const paletteType = Config.options?.appearance?.palette?.type ?? "auto"
+            const paletteMode = Config.options?.appearance?.palette?.mode ?? "dark"
             const command = [Directories.wallpaperSwitchScriptPath, "--noswitch"]
             if (paletteType !== "auto")
                 command.push("--type", paletteType)
+            if (paletteMode === "dark" || paletteMode === "light")
+                command.push("--mode", paletteMode)
--- a/scripts/colors/apply-gtk-theme.sh
+++ b/scripts/colors/apply-gtk-theme.sh
@@ -197,3 +197,3 @@
 generate_kdeglobals() {
     local icon_theme font_raw mono_raw font_name font_size mono_name mono_size
-    icon_theme=$(gsettings get org.gnome.desktop.interface icon-theme 2>/dev/null | tr -d "'")
+    icon_theme=$(gsettings get org.gnome.desktop.interface icon-theme 2>/dev/null | tr -d "'" || true)
@@ -201,4 +201,4 @@
     # Read current GTK fonts so wallpaper theme changes don't reset user fonts
-    font_raw=$(gsettings get org.gnome.desktop.interface font-name 2>/dev/null | tr -d "'")
-    mono_raw=$(gsettings get org.gnome.desktop.interface monospace-font-name 2>/dev/null | tr -d "'")
+    font_raw=$(gsettings get org.gnome.desktop.interface font-name 2>/dev/null | tr -d "'" || true)
+    mono_raw=$(gsettings get org.gnome.desktop.interface monospace-font-name 2>/dev/null | tr -d "'" || true)
     if [[ -n "$font_raw" ]]; then
@@ -576,4 +576,5 @@
 [General]
 ColorScheme=Darkly
 Name=Darkly
+TerminalApplication=kitty
 shadeSortColumn=true
@@ -868,2 +869,2 @@
     local current_icon
-    current_icon=$(gsettings get org.gnome.desktop.interface icon-theme 2>/dev/null | tr -d "'")
+    current_icon=$(gsettings get org.gnome.desktop.interface icon-theme 2>/dev/null | tr -d "'" || true)
@@ -872,2 +873,2 @@
     local current_cursor
-    current_cursor=$(gsettings get org.gnome.desktop.interface cursor-theme 2>/dev/null | tr -d "'")
+    current_cursor=$(gsettings get org.gnome.desktop.interface cursor-theme 2>/dev/null | tr -d "'" || true)
